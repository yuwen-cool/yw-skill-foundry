---
name: [skill-name]
description: "[What it does in 1 sentence]. Use when [trigger condition 1], [trigger condition 2], or [trigger condition 3]. Triggers on [keyword-zh] / [keyword-en] / [fuzzy-variant]."
---

# [Skill Name]

<!-- PRODUCTION TEMPLATE:
     For a first version, use scaffold-starter.md instead.
     Keep only modules that match the skill's actual complexity; delete every
     unused section. Empty modules are context tax, not rigor. Canonical track
     rules: references/invariants.md in YW SkillFoundry. -->

<!-- Opening Statement: 1-2 sentences that reframe AI's default behavior.
     Generation skill → value statement: "If it could have been generated by a default prompt, it is not good enough."
     Methodology skill → cognitive axiom: "Most bugs are misread, not miswritten."
     Delete this comment after writing. -->

[AI defaults to X. This skill ensures Y instead.]

## Outcome Contract

- **Outcome**: [What the skill produces — specific, verifiable]
- **Done when**: [Completion criteria AI can check without asking]
- **Output**: [Deliverable format: file type, structure, location]

<!-- Put deterministic requirements in evals/contract.json and run
     yw-skill-foundry/scripts/contract_eval.py. Put subjective criteria in
     manual_checks; ungraded manual criteria must never count as pass. -->

## Capability Circle

<!-- Only if the skill has real refusal boundaries. Delete if everything in-domain is in-scope. -->

**Can do**: [list specific tasks within scope]

**Can do with conditions**: [tasks that need user input or external resources]

**Cannot do**: [tasks to refuse at intake — name them, do not silently attempt]

## Mode Router

<!-- Only if 2+ distinct execution paths exist. Delete if single-path. -->

| Signal | Mode |
|---|---|
| "[user phrase 1]" | **[Mode A]** — [one-line description by feeling, not specs] |
| "[user phrase 2]" | **[Mode B]** — [one-line description by feeling, not specs] |

## Workflow

### Step 1: [Verb] — [what this step produces]

<!-- Each step: action verb → specific output → reference to load if needed. -->

[Instructions for this step.]

Read `references/[relevant-file].md` when [condition].

### Step 2: [Verb] — [what this step produces]

[Instructions for this step.]

### Step 3: [Verb] — [what this step produces]

[Instructions for this step.]

## Hard Rules

<!-- Three-tier register — do NOT make every rule a MUST (stacked MUSTs dilute
     the ones that matter):
     Tier 1 (non-negotiable / irreversible / safety) → MUST / NEVER / No exceptions.
     Tier 2 (everything else) → imperative + reason: "Do X, because Y."
     Tier 3 (never) → "consider" / "try to" / "you might want to". -->

- [Non-negotiable constraint] **MUST** [verifiable behavior]. No exceptions.
- [Safety boundary] **NEVER** [banned behavior]. [Why in ≤10 words].
- [Ordinary rule as imperative]: [Do X], because [reason in ≤15 words].

## Anti-Rationalization

<!-- Only if testing showed AI actually taking shortcuts. Delete if empty — hypothetical entries teach nothing. -->

| AI thinks | AI must do instead |
|---|---|
| "[rationalization phrase]" | [specific corrective action] |
| "[shortcut phrase]" | [specific corrective action] |

## Output Format

<!-- Provide a seed template if the output is structured.
     Mark editable regions with [brackets]. -->

```
[output-format-example]
```

## Reference Map

<!-- Sort by probable access order, not alphabetically.
     Always-needed files first; conditional files last. -->

| File | What it provides | Load when |
|---|---|---|
| `references/[file-1].md` | [content summary] | [step or condition] |
| `references/[file-2].md` | [content summary] | [step or condition] |

## Gotchas

<!-- Real failure stories only. No hypotheticals. Add after each test round. -->

| What happened | Rule |
|---|---|
| [Specific failure from testing] | [Lesson learned — concrete, not abstract] |
