# 迭代记录（本地专用，不进公开仓库）

这个文件本身不会被提交——`.gitignore` 已经排除了 `log.md`。首次使用时复制一份：

```bash
bash scripts/ensure-log.sh
# 等价于（仅当 log.md 不存在时才执行）：cp log.md.example log.md
```

以后每次改完 skill、发现一个真实失败模式，就往下面加一行，不用管格式好不好看：

```markdown
## 2026-07-14
### 场景：给什么任务写/改的 skill
### 发现：AI 具体在哪里没按预期做
### 改进：改了哪一条规则或例子
```

已经泛化、不含项目名、人名、邮箱、路径、会话 ID 的结论，可以挑出来写进 `SKILL.md` 的 Gotchas 表；完整的公开边界见 `PRIVACY.md`。
